# 時間加權成長率計算方法

> 📅 **最後更新**：2025-11-05  
> 📦 **版本**：v1.7.0  
> 👨‍💻 **維護者**：AI 協作開發

---

## 📚 閱讀指南

本文件說明本工具的時間加權成長率計算方法，包含基礎概念與進階數學推導。

- **🟢 基礎版**：適合一般投資人
  - 理解時間加權的核心概念
  - 學習如何設定參數
  - 查看實務應用範例

- **🔵 進階版**：適合專業用戶與研究人員
  - 完整數學公式推導
  - 指數衰減權重原理
  - 統計學與優化方法

---

## 目錄

1. [核心概念](#核心概念)
2. [為什麼需要時間加權](#為什麼需要時間加權)
3. [計算方法](#計算方法)
4. [參數設定](#參數設定)
5. [實務範例](#實務範例)
6. [進階理論](#進階理論)
7. [常見問題](#常見問題)

---

# 🟢 基礎版

## 核心概念

### 什麼是時間加權？

時間加權（Time Weighting）是一種給予**近期數據更高權重**的計算方法。

**簡單比喻**：

想像你要預測明年的天氣，你會怎麼參考歷史資料？

- **等權重方法**：過去 5 年的氣溫數據，每年權重相同（各 20%）
- **時間加權方法**：最近的 2 年權重較高（例如 40%），較早期的權重較低（例如 10%）

哪一種更準確？通常是**時間加權方法**，因為氣候變遷的趨勢在近年更明顯。

### 應用在股票估值

計算股票成長率時，我們也面臨同樣的問題：

**傳統方法（等權重）**：
```
假設過去 5 年 EPS：
2019: $10
2020: $12  
2021: $14
2022: $16
2023: $20

等權重計算：每年權重 20%
```

**時間加權方法**：
```
時間加權分配（recent_weight_ratio = 0.6）：
2019: 權重 5.8%   ← 較早期，權重低
2020: 權重 10.5%
2021: 權重 19.0%
2022: 權重 34.4%  ← 近期，權重高
2023: 權重 60.3%  ← 最近期，權重最高

近 2 年加總：60.3% + 34.4% = 94.7% ≈ 60% 目標 ✓
```

---

## 為什麼需要時間加權？

### 優點 1：更準確反映當前趨勢

公司的成長動能可能在改變：

**例子：台積電**
- 2019-2020：穩定成長期（成長率 10-15%）
- 2021-2023：AI 芯片爆發期（成長率 25-35%）

使用等權重會**稀釋近期的高成長**，低估公司潛力。

### 優點 2：對突發事件反應更快

**疫情影響範例**：

某旅遊股的 EPS：
```
2019: $5.0  (正常)
2020: $1.0  (疫情重創) ← 異常值
2021: $2.5  (恢復中)
2022: $4.0  (接近正常)
2023: $5.5  (超越疫情前)

等權重：會過度受 2020 年異常值影響
時間加權：2020 年權重低，更準確反映恢復趨勢
```

### 優點 3：符合統計學原理

**EWMA（指數加權移動平均）**在金融領域廣泛應用：
- 風險管理（VaR 計算）
- 技術分析（EMA 指標）
- 波動率預測（GARCH 模型）

時間加權成長率是 EWMA 在估值領域的應用。

---

## 計算方法

### 步驟 1：計算權重分配

本工具使用**指數衰減權重**：

**公式（不需記住，系統自動計算）**：
- 最近一年權重最高
- 越早期權重越低
- 按指數遞減

**預設設定**：
- `recent_weight_ratio = 0.6`
- 表示：近 40% 的數據（2年）佔 60% 的權重

### 步驟 2：加權計算成長率

假設 5 年 EPS 數據：

| 年份 | EPS | 權重 | 加權 EPS |
|------|-----|------|----------|
| 2019 | $10 | 5.8% | $0.58 |
| 2020 | $12 | 10.5% | $1.26 |
| 2021 | $14 | 19.0% | $2.66 |
| 2022 | $16 | 34.4% | $5.50 |
| 2023 | $20 | 30.3% | $6.06 |

**加權成長率計算**：
```
從加權 EPS 序列計算複合成長率
考慮權重調整後的時間序列
```

---

## 參數設定

### recent_weight_ratio（近期權重比例）

**預設值**：`0.6`（60%）

**意義**：
- 60% = 「近期 40% 的數據」佔「60% 的總權重」
- 例如：5 年數據，近 2 年（40%）佔 60% 權重

**如何選擇**：

| 情境 | 建議設定 | 說明 |
|------|----------|------|
| **穩定成長股**<br>（台積電、中華電） | 0.5 - 0.6 | 平衡歷史與近期 |
| **高成長股**<br>（聯發科、AI 概念股） | 0.7 - 0.8 | 更重視近期動能 |
| **週期股**<br>（航運、鋼鐵） | 0.4 - 0.5 | 考慮完整週期 |
| **傳統產業**<br>（食品、紡織） | 0.5 | 穩定，等權重可能更好 |

### 如何調整參數？

在 `portfolio_analyzer/config.py` 中修改：

```python
# 時間加權設定
ENABLE_TIME_WEIGHTING = True      # 啟用時間加權
RECENT_WEIGHT_RATIO = 0.6          # 近期權重比例（預設 60%）
```

**建議**：
- 初學者：使用預設值 0.6
- 進階用戶：根據投資風格調整

---

## 實務範例

### 範例 1：台積電（穩定高成長）

**歷史 EPS**（2019-2023）：
```
2019: $10.0
2020: $12.5  (+25%)
2021: $15.6  (+25%)
2022: $19.5  (+25%)
2023: $24.4  (+25%)
```

**等權重計算**：
- 平均成長率：25%
- 每年貢獻相同

**時間加權計算（ratio=0.6）**：
- 加權成長率：約 25%（因為成長率穩定，差異不大）
- 但如果最近 2 年加速成長，會更敏感

### 範例 2：某復甦股

**歷史 EPS**：
```
2019: $5.0
2020: $2.0  (-60%, 疫情衝擊)
2021: $3.5  (+75%, 恢復中)
2022: $5.0  (+43%, 接近正常)
2023: $6.5  (+30%, 超越疫情前)
```

**等權重計算**：
- 5 年 CAGR：約 +5.4%
- 嚴重受 2020 年拖累

**時間加權計算（ratio=0.6）**：
- 加權成長率：約 +12%
- 更準確反映最近 3 年的復甦趨勢

**結論**：時間加權更能捕捉公司的實際恢復動能。

### 範例 3：某衰退股

**歷史 EPS**：
```
2019: $8.0
2020: $7.5  (-6%)
2021: $6.8  (-9%)
2022: $5.5  (-19%)
2023: $4.0  (-27%)
```

**等權重計算**：
- 5 年 CAGR：約 -13%

**時間加權計算（ratio=0.6）**：
- 加權成長率：約 -20%
- 更早警示衰退加速的趨勢

---

# 🔵 進階版

## 數學原理

### EWMA（指數加權移動平均）

時間加權成長率基於 EWMA 理論：

**標準 EWMA 公式**：

$$
\bar{x}_t = \lambda x_t + (1-\lambda) \bar{x}_{t-1}
$$

其中：
- $\bar{x}_t$ = t 時刻的加權平均
- $\lambda$ = 衰減參數（0 < λ < 1）
- $x_t$ = t 時刻的觀測值

**遞迴展開**：

$$
\bar{x}_t = \lambda \sum_{i=0}^{t-1} (1-\lambda)^i x_{t-i}
$$

權重隨時間指數衰減。

### 本工具的實作

**離散時間權重公式**：

$$
w_i = \frac{\exp(-\lambda \cdot (n-1-i))}{\sum_{j=0}^{n-1} \exp(-\lambda \cdot (n-1-j))}
$$

其中：
- $w_i$ = 第 i 年的權重（i = 0 為最早，i = n-1 為最新）
- $\lambda$ = 衰減參數
- $n$ = 總年數

**標準化**：確保 $\sum_{i=0}^{n-1} w_i = 1$

### 衰減參數 λ 的計算

**目標**：使近期 r% 的數據佔總權重的 p%

**數學推導**：

定義近期數據為最後 $k = \lceil n \cdot r \rceil$ 筆。

要求：
$$
\frac{\sum_{i=n-k}^{n-1} w_i}{\sum_{i=0}^{n-1} w_i} = p
$$

由於已標準化分母為 1：
$$
\sum_{i=n-k}^{n-1} w_i = p
$$

代入權重公式：
$$
\sum_{i=n-k}^{n-1} \frac{\exp(-\lambda \cdot (n-1-i))}{\sum_{j=0}^{n-1} \exp(-\lambda \cdot (n-1-j))} = p
$$

令 $m = n - 1 - i$，則：
$$
\frac{\sum_{m=0}^{k-1} \exp(-\lambda \cdot m)}{\sum_{m=0}^{n-1} \exp(-\lambda \cdot m)} = p
$$

利用幾何級數公式：
$$
\sum_{m=0}^{N} \exp(-\lambda \cdot m) = \frac{1 - \exp(-\lambda \cdot (N+1))}{1 - \exp(-\lambda)}
$$

得到方程：
$$
\frac{1 - \exp(-\lambda \cdot k)}{1 - \exp(-\lambda \cdot n)} = p
$$

**求解方法**：使用數值方法（Brent's method）求根。

### 實作細節

```python
from scipy.optimize import brentq

def _calculate_lambda(n: int, recent_weight_ratio: float) -> float:
    """
    計算衰減參數 λ
    
    Args:
        n: 總數據點數
        recent_weight_ratio: 近期權重比例 (0 < p < 1)
    
    Returns:
        float: 衰減參數 λ
    """
    # 確定近期數據點數（40% 的數據點）
    k = max(1, int(np.ceil(n * 0.4)))
    
    def weight_equation(lam):
        # 方程：近期 k 點權重和 = recent_weight_ratio
        numerator = 1 - np.exp(-lam * k)
        denominator = 1 - np.exp(-lam * n)
        return (numerator / denominator) - recent_weight_ratio
    
    # 使用 Brent's method 求根
    lambda_opt = brentq(weight_equation, 0.01, 10.0)
    return lambda_opt
```

### 權重計算

```python
def _calculate_exponential_weights(n: int, recent_weight_ratio: float) -> np.ndarray:
    """
    計算指數衰減權重
    
    Returns:
        np.ndarray: 長度為 n 的權重陣列，總和為 1.0
    """
    lam = _calculate_lambda(n, recent_weight_ratio)
    
    # 計算未標準化權重
    indices = np.arange(n)
    weights = np.exp(-lam * (n - 1 - indices))
    
    # 標準化
    weights = weights / np.sum(weights)
    
    return weights
```

### 時間加權成長率計算

```python
def _calculate_time_weighted_growth_rate(
    eps_series: pd.Series,
    recent_weight_ratio: float
) -> float:
    """
    計算時間加權成長率
    
    Args:
        eps_series: EPS 時間序列（已排序，最舊 → 最新）
        recent_weight_ratio: 近期權重比例
    
    Returns:
        float: 年化加權成長率
    """
    eps_values = eps_series.values
    n = len(eps_values)
    
    if n < 2:
        return 0.0
    
    # 計算權重
    weights = _calculate_exponential_weights(n, recent_weight_ratio)
    
    # 計算加權 log returns
    log_returns = np.log(eps_values[1:] / eps_values[:-1])
    
    # 權重也需要調整（使用相鄰權重的平均）
    weights_for_returns = (weights[:-1] + weights[1:]) / 2
    weights_for_returns = weights_for_returns / np.sum(weights_for_returns)
    
    # 加權平均 log return
    weighted_log_return = np.sum(log_returns * weights_for_returns)
    
    # 轉換為年化百分比
    growth_rate = (np.exp(weighted_log_return) - 1) * 100
    
    return growth_rate
```

## 理論基礎

### 1. 為什麼使用指數衰減？

**理論依據**：

1. **時間序列平穩性**（Stationarity）
   - 金融數據通常非平穩
   - 近期數據對未來預測更有參考價值

2. **記憶長度**（Memory Length）
   - 指數衰減提供有限但連續的記憶
   - 參數 λ 控制記憶長度

3. **數學性質優良**
   - 遞迴計算效率高
   - 參數調整直觀
   - 數值穩定性好

### 2. 與其他加權方法比較

| 方法 | 權重分配 | 優點 | 缺點 |
|------|----------|------|------|
| **等權重** | 均勻分配 | 簡單、穩定 | 忽略時間趨勢 |
| **線性衰減** | 線性遞減 | 直觀 | 不符合實證分布 |
| **指數衰減** | 指數遞減 | 理論扎實、靈活 | 需要參數調整 |
| **二元加權** | 只用近期/遠期 | 極簡 | 資訊損失大 |

**實證研究**（金融時間序列）：
- 指數衰減在波動率預測準確度最高
- GARCH 模型也採用類似概念

### 3. 參數敏感性分析

**λ 對權重分配的影響**：

| λ | 最新年權重 | 最舊年權重 | 近2年權重和 |
|---|-----------|-----------|------------|
| 0.1 | 23% | 18% | 45% |
| 0.3 | 31% | 12% | 57% |
| 0.5 | 38% | 9% | 66% |
| 0.7 | 44% | 6% | 73% |
| 1.0 | 51% | 4% | 81% |

**recent_weight_ratio 的對應**：

```
ratio = 0.5 → λ ≈ 0.28
ratio = 0.6 → λ ≈ 0.45
ratio = 0.7 → λ ≈ 0.69
ratio = 0.8 → λ ≈ 1.08
```

### 4. 數值穩定性

**潛在問題**：

當 λ 很大時，$\exp(-\lambda \cdot n)$ 可能趨近 0，造成數值不穩定。

**解決方案**：

1. **限制 λ 範圍**：`0.01 ≤ λ ≤ 10.0`
2. **使用 log-sum-exp trick**（如需要）：
   ```python
   # 不穩定：
   z = exp(x) / sum(exp(x_i))
   
   # 穩定版本：
   m = max(x_i)
   z = exp(x - m) / sum(exp(x_i - m))
   ```

3. **early stopping**：當權重 < 1e-6 時截斷

## 統計檢定

### 時間加權 vs 等權重：哪個更好？

**檢定方法**：

1. **預測準確度檢定**
   - 計算未來 1 年 EPS 的預測誤差
   - 比較兩種方法的 MSE（均方誤差）

2. **Diebold-Mariano Test**
   - 統計檢定兩種預測方法是否有顯著差異

**實證結果**（需進行 A/B 測試）：

```
待完成 P2-37：時間加權效果驗證
- 使用歷史數據回測
- 比較預測準確率
- 調整最佳 recent_weight_ratio
```

---

## 實作建議

### 1. 何時使用時間加權？

**適合的情境**：
- ✅ 公司處於成長加速階段
- ✅ 產業技術快速變革
- ✅ 近期發生重大事件（疫情後復甦）
- ✅ 高成長科技股

**不適合的情境**：
- ❌ 穩定成長的傳統產業
- ❌ 週期股（需要看完整週期）
- ❌ 數據品質不佳（近期數據不可靠）

### 2. 參數調校策略

**階段 1：使用預設值**
```python
RECENT_WEIGHT_RATIO = 0.6  # 適用大多數情況
```

**階段 2：根據產業調整**
```python
# 科技股/高成長股
RECENT_WEIGHT_RATIO = 0.7

# 傳統產業/週期股
RECENT_WEIGHT_RATIO = 0.5

# 極端成長股（AI、生技）
RECENT_WEIGHT_RATIO = 0.8
```

**階段 3：個股優化**
- 回測歷史預測準確度
- 找出最佳 ratio 值
- 記錄在個股配置中

### 3. 監控與調整

**定期檢查**（每季）：
1. 比較時間加權 vs 等權重的預測結果
2. 檢視 ratio 設定是否仍適用
3. 根據產業變化調整參數

**警示訊號**：
- 時間加權預測持續失準
- 成長率突然大幅改變
- 出現明顯異常值

---

## 常見問題

### Q1: 時間加權會不會過度反應近期變化？

**A**: 可能會。這就是為什麼需要設定合理的 `recent_weight_ratio`。

- 設定 0.6（預設）：平衡歷史與近期
- 設定過高（> 0.8）：可能過度敏感
- 建議搭配**敏感性分析**，測試不同 ratio 的結果

### Q2: 等權重和時間加權差異大嗎？

**A**: 取決於數據特性。

- **成長穩定**：差異小（< 2%）
- **趨勢改變**：差異可能 > 10%
- **突發事件**：差異可能 > 20%

建議同時查看兩種結果。

### Q3: 可以用時間加權計算其他指標嗎？

**A**: 可以！時間加權概念可應用於：

- 加權 ROE（反映近期獲利能力）
- 加權負債比（反映財務狀況改善）
- 加權毛利率（反映產品競爭力變化）

但需要評估是否合理。

### Q4: 時間加權會影響 DCF 估值結果嗎？

**A**: 會，因為成長率改變。

**範例**：
```
等權重成長率：15%
時間加權成長率：18%（近期加速）

DCF 內在價值：
- 等權重：$1,000
- 時間加權：$1,150（+15%）
```

這也是為什麼需要做**敏感性分析**。

### Q5: 數據不足 5 年怎麼辦？

**A**: 系統會自動處理。

- **3-4 年數據**：仍可使用時間加權，權重調整更極端
- **< 3 年數據**：建議使用等權重，數據太少不適合加權
- 系統會自動降低資料品質評分

---

## 參考資源

### 學術論文

1. **EWMA 原理**
   - J.P. Morgan (1996), *RiskMetrics Technical Document*
   - 介紹 EWMA 在風險管理的應用

2. **時間序列預測**
   - Box, G. E. P., & Jenkins, G. M. (1970), *Time Series Analysis: Forecasting and Control*
   - 經典時間序列教科書

3. **波動率預測**
   - Engle, R. F. (1982), "Autoregressive Conditional Heteroscedasticity with Estimates of the Variance of United Kingdom Inflation"
   - ARCH/GARCH 模型基礎（諾貝爾經濟學獎）

### 實務應用

1. **技術分析**
   - EMA（指數移動平均線）= EWMA 的應用
   - 常用參數：12 日、26 日 EMA

2. **風險管理**
   - Basel II/III 監管框架
   - RiskMetrics 標準（J.P. Morgan）

3. **量化投資**
   - 因子模型中的動態權重
   - Alpha 預測中的時間衰減

---

## 總結

### 核心要點

1. **時間加權給予近期數據更高權重**
   - 更能反映當前趨勢
   - 對突發事件反應更快

2. **基於 EWMA 理論**
   - 金融領域廣泛應用
   - 數學原理扎實

3. **參數可調整**
   - `recent_weight_ratio` 控制近期權重比例
   - 預設 0.6 適用大多數情況

4. **需要謹慎使用**
   - 不是萬能方法
   - 建議搭配等權重結果比較

### 下一步

- [x] **P2-32**: 實作指數衰減時間加權函式 ✅
- [x] **P2-33**: 時間加權參數優化與計算 ✅
- [x] **P2-34**: 時間加權配置與 UI 顯示 ✅
- [x] **P2-35**: 建立時間加權單元測試 ✅
- [ ] **P2-36**: 時間加權視覺化與文件（本文件 ✅）
- [ ] **P2-37**: 時間加權效果驗證與調優

**相關文件**：
- [主要計算方法文件](CALCULATION_METHODS.md)
- [公式速查表](FORMULA_REFERENCE.md)
- [專案說明](README.md)

---

**最後更新**: 2025-11-05  
**版本**: v1.7.0  
**維護者**: AI 協作開發
